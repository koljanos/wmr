Add group 'weather':
# addgroup weather
# addgroup <user> weather

Copy wmr.rules into /etc/udev and symlink:
# cp wmr.rules /etc/udev
# cd etc/udev/rules.d; ln -s ../wmr.rules 025_wmr.rules

Run wmr-oregon.
